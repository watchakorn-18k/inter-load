# Check Types

Run type checking across both frontend (TypeScript/Svelte) and backend (Rust).

## Instructions

1. Run Svelte/TypeScript type check: `bun run check`
2. Run Rust type check: `cd src-tauri && cargo check`
3. Report results from both — list any errors with file paths and line numbers
4. If there are errors, suggest or apply fixes
